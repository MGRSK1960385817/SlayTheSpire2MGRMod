# MGR 项目 Agent 工作规则

本文件适用于整个 `mgrmod` 仓库。任何 Codex agent 在分析、规划或实施与本项目有关的新需求前，都必须遵守以下流程。

## 一、开始任何任务前

1. 先完整阅读 `docs/总览目录.md`。
2. 再完整阅读 `docs/当前工作项.md`，确认已有工作、待验证事项、阻塞和共享文件。
3. 根据总览中的分类，继续阅读与任务直接相关的权威文档；不得只依赖聊天摘要、旧结论或文件名猜测。
4. 如果用户提出的是新需求或新规划，先判断它与现有工作项是新增、补充、替代还是冲突，再开始实施。
5. 如果文档与源码或用户最新要求冲突，以用户最新要求和实际运行行为为准，并在本次任务中同步修正文档。

## 二、工作项与文档闭环

1. 需要修改代码、资源、数值、文本、构建或发布内容的任务，实施前先在 `docs/当前工作项.md` 新增或更新工作项。
2. 每个独立任务都必须至少新增或更新一份 `docs/` 文档；不得只修改实现而不留下原因、方案或验证记录。
3. 小改动优先更新现有权威文档和 `docs/records/MGR错误修复与表现改进记录.md`，不要无故创建零散文档。
4. 跨系统设计、迁移、长链路诊断或大范围回归应在 `docs/records/` 建立专项记录，并从当前工作项链接。
5. 完成任务时更新当前工作项的状态、验证结果、下一步和关联文档；已经闭环的事项应从“进行中”移出。
6. 变更记录必须按实际性质区分 BUG 修复、数值平衡、性能优化、表现改进、功能改进或文档流程改进。
7. 只有用户确认内容已经实际发布后，才能在对应变更记录标题后添加 `✅`。

## 三、路径与验证

1. 新文档必须放入总览规定的分类目录；`docs/` 根目录只保留入口文件和必要的目录控制文件。
2. 移动或重命名文档后，必须同步更新 README、脚本、登记数据及其他文档中的引用。
3. 涉及内容登记、卡牌、遗物、资源或本地化时，按需运行：

   ```powershell
   pwsh -NoProfile -File docs/tools/Validate-MgrContent.ps1
   ```

4. 任务结束前按 `docs/总览目录.md` 的检查单完成文档、链接、验证和发布状态核对。
5. 除非用户明确要求，不要因为文档或普通代码任务自动导出 PCK、部署到游戏目录或发布内容。
