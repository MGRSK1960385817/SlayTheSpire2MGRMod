# MGR 错误修复、数值平衡与表现改进记录

本文件自 20260817 起持续维护，每次完成 BUG 修复、数值平衡或表现改进后均追加一条记录，并通过“类型”字段准确归类，不再将所有修改统称为 BUG。

发布状态规则：标题末尾带有“✅”的记录表示已经发布到云端，未打勾的记录表示尚未发布；每次发布新内容时必须同步为对应记录打勾，上传文本时只选取未打勾记录，避免重复上传已经发布的内容。

## 20260817-01 ✅

日期：20260817。

类型：BUG 修复。

修改内容：SL 保存并重进战斗后，“时间线之东”此前永久增加的音符生成数量会恢复为基础值。

原因定位：永久成长只修改了不会被战斗存档序列化的 `Notes` 动态变量，重建卡牌实例时因此丢失。

修复与改善：新增可序列化的 `CurrentNotes` 永久数值，并让牌组原卡与所有战斗副本同步通过该数值更新音符生成量。

## 20260817-02 ✅

日期：20260817。

类型：表现改进。

修改内容：DAYBREAK FRONTLINE 移除演奏队中的诅咒时，卡牌会在原位静止到结算结束后突然消失。

原因定位：中央消耗动画只创建了临时卡面，却没有接管演奏队原卡面的显示与起始坐标。

修复与改善：动画开始时隐藏演奏队原卡及其悬浮交互，并让临时卡从原卡的准确屏幕位置飞向中央后播放消耗动画。

## 20260817-03 ✅

日期：20260817。

类型：BUG 修复。

修改内容：涅染夜曲未能稳定增加诅咒音符以及万象音符中诅咒分量的生命回复。

原因定位：涅染夜曲加成的读取与计算分散在多条音符结算路径中，且诅咒音符的显示数字只读取基础回复量。

修复与改善：统一使用涅染夜曲治疗解析函数处理普通、万象及共享结算，并让诅咒音符显示同步计入额外回复量。

## 20260817-04 ✅

日期：20260817。

类型：表现改进。

修改内容：卡农形态在人物背后的轮盘光效始终采用固定的紫色与青色，色彩表现较单一。

原因定位：轮盘的光晕、圆环、刻度与月相描边均使用写死的单色值绘制。

修复与改善：将轮盘各层改为随角度和时间流动的彩色渐变，并保留原有结构、显隐、脉冲及旋转节奏。

## 20260817-05 ✅

日期：20260817。

类型：表现改进。

修改内容：ElectricAngel 在回合开始生成卡牌时会将卡牌瞬间放入手牌，缺少清晰的获得反馈。

原因定位：回合开始阶段直接调用原生生成牌入手命令，其手牌飞入动画几乎没有可读的展示时间。

修复与改善：在真实入手前新增从人物电光位置显现、中央停留并飞向手牌的专属卡牌动画，抵达后再完成牌堆结算。

## 20260818-01 ✅

日期：20260818。

类型：数值平衡。

修改内容：小天使触发时一次抽取四张牌，补牌收益高于当前设计目标。

原因定位：遗物的 `Cards` 动态变量基础值设置为四，并同时驱动实际抽牌数与描述数字。

修复与改善：将小天使的基础抽牌数从四张下调为三张，使实际效果及所有语言的动态描述同步更新。

## 20260818-02 ✅

日期：20260818。

类型：BUG 修复与音频表现改进。

修改内容：有玩家在人物选择界面把光标移到 MGR 人物并点击后，游戏会立即失去响应，严重时直接闪退。从玩家的视频看，卡住发生在人物选择音效真正播放之前：界面没有继续加载人物图片和背景，也没有听到任何选择音效。最初收到的日志片段只包含 RitsuLib 反射枚举警告、公共资源预加载和窗口焦点变化，缺少真正的异常堆栈，因此无法只凭这段日志证明是图片、背景或人物数据导致的问题。

后续还发现了几项与同一套音频流程有关的表现问题：人物选择音效虽然能够播放，但音量明显偏小；音符、和弦、书写声和故障声在部分场景中几乎听不到；两张能力牌所使用的书写声和故障声只播放一次，没有按预期持续循环。这些现象并非同一个错误，但都来自 MGR 自定义音频从原始文件播放迁移到 FMOD Studio 事件播放的过程中，因此统一进行了排查和修复。

为什么会出现错误：

1. 旧实现把角色选择音效作为 OGG 资源交给 RitsuLib 的 `AudioSource.ResourceFile`、`StreamingResourceMusic` 和底层 `load_file_as_sound` 流程处理。人物选择发生在界面切换的关键路径上，如果原始音频文件的解析、流式加载或声音对象创建长时间不返回，主线程就无法继续执行后续人物图片、背景和界面状态更新。玩家看到的结果便是“点击人物后整个游戏定住”，而且因为调用尚未返回，连音效本身也不会响。这种故障未必会立即产生一条清晰的托管异常，所以残缺日志中可能只留下卡死前最后完成的普通初始化信息。

2. 旧的五个 OGG 文件经过资源导入、运行时文件读取和声音对象创建后才进入播放流程，角色选择音效又位于对卡顿极其敏感的 UI 操作中。即使音频内容本身没有损坏，这条运行时加载链也比直接播放已经构建好的 FMOD 事件多出若干可能阻塞或失败的环节。为了消除这一类不可控因素，正式方案不再尝试给 `load_file_as_sound` 增加等待或超时补丁，而是移除了 MGR 运行时对原始 OGG 加载链的依赖。

3. 音频迁移到 FMOD Bank 后，最初仍沿用了旧的原始文件播放音量，例如音符、和弦和循环音效原先使用的较小倍率。FMOD Studio 事件会经过事件自身、Bus 和游戏总线的音量共同处理，旧倍率直接套用后会造成二次衰减，因此出现了“事件已经成功启动，但实际听起来像没有播放”的情况。对源音频响度的检查也表明，音符、和弦、书写声和故障声本身的平均响度低于人物选择音效，需要分别补偿，而不能只提高整个游戏的音量。

4. 循环音效还包含两个独立问题。首先，RitsuLib 的循环播放选项默认认为事件中存在一个名为 `loop` 的 FMOD 参数，并会在启动时设置该参数；MGR 的自定义事件没有这个参数，所以日志出现了无法设置 `loop` 参数的错误。其次，保留一个 FMOD 事件实例只代表它可以在以后被停止和释放，并不会自动让事件时间轴从结尾回到开头。原先的 Writing 和 Glitch 事件只是普通的单次声音，时间轴中没有 Loop Region，因此即使成功取得播放句柄也只会播放一次。

5. `.bank` 和 `GUIDs.txt` 不属于 Godot 通常自动导入的资源类型。如果脚本打包时没有继承项目导出预设中的非资源文件过滤规则，开发目录中存在这些文件也没有意义，最终 PCK 仍可能缺少 Bank 或 GUID 映射。这样代码里的事件路径就无法在玩家环境中正确解析，因此必须把打包规则也视为修复的一部分，而不只是修改播放代码。

最终修复方式：

1. 将全部五个 MGR 音频制作并构建到 `MGR.bank`，同时随 Mod 提供对应的 `GUIDs.txt`。目前使用的事件分别为 `event:/MGR/sfx/MGR_charselect`、`event:/MGR/sfx/NoteChannel`、`event:/MGR/sfx/Chord`、`event:/MGR/sfx/Writing` 和 `event:/MGR/sfx/Glitch`。Mod 初始化时通过 RitsuLib 提供的 FMOD Studio Bank 延迟注册和 GUID 映射注册接口加载它们，业务代码只负责按事件路径播放，不再临时把 OGG 转换成声音对象。

2. 人物选择音效改为直接写入 `CharacterAudioAssetSet` 的 FMOD 事件路径，并由游戏原有的 `SfxCmd.Play` 流程播放。原先专门把人物选择音效重定向到 ResourceFile 的补丁已经移除。这样点击 MGR 时执行的是与原版人物选择音效同类的事件调用，图片和背景加载不再等待 MGR 原始音频文件的同步处理。

3. 音符和和弦等一次性音效统一改为 `SfxCmd.Play(eventPath, volume)`；书写声和故障声仍保留可停止、可释放的 `IAudioHandle` 生命周期，以便能力结束或界面租约释放时及时停止。自定义循环事件明确设置 `UsesLoopParameter = false`，避免再向不存在的 `loop` 参数写值。

4. 在 FMOD 工程中为 Writing 和 Glitch 事件的实际声音时长添加 Loop Region，使循环行为由 Bank 内的事件时间轴正确定义。Writing 的循环区间覆盖约 12.701 秒，Glitch 的循环区间覆盖约 1.736 秒。重新构建后的 Bank 已替换进 Mod，因此循环句柄负责控制开始和停止，FMOD 事件自身负责从结尾回到开头。以后如果重新打开 FMOD 工程并构建 Bank，必须保留这两个 Loop Region，否则“只播放一次”的问题会重新出现。

5. 为 MGR 事件增加了局部音量倍率逻辑，只匹配 `event:/MGR/` 路径，不会放大原版游戏或其他 Mod 的音效。人物选择音效拥有独立倍率，音符、和弦、书写声和故障声也保留各自的基础倍率；以后可直接在 `MgrAudio` 的集中参数中调整，不必重新编辑每个调用位置。这样既能补偿 Bank 事件与源文件响度的差异，也避免为了一个较安静的 MGR 音效改变全局 Bus。

6. Godot 导出预设中明确包含 `MGRMod/audio/*.bank` 和 `MGRMod/audio/GUIDs.txt`，并对实际导出的 PCK 进行了检查，确认其中 Bank 与 GUID 文件的内容和哈希与构建产物一致。因此，无论使用编辑器导出还是项目脚本调用该导出预设，最终安装包都会携带事件播放所需文件。

7. 排查期间曾在 Bank 注册、GUID 映射、每次准备播放、播放调用返回、循环句柄创建和停止等阶段加入详细诊断日志。它们证明 Bank 已成功注册、五个事件均已映射，并且音符和和弦的播放调用能在极短时间内返回；同时也直接暴露了缺少 `loop` 参数的问题。玩家确认新版本不再因选择 MGR 而崩溃后，这批高频额外日志已经移除，只保留 Bank 注册结果和真正失败时需要的警告，避免长期污染正常日志。

修复解决的问题：人物选择不再经过可能阻塞的 `ResourceFile/load_file_as_sound` 原始文件加载链，已经解决点击 MGR 后无音效、无图片、无背景并卡死或闪退的核心问题；五个音频现在统一由同一套 FMOD Bank 和事件路径管理；较安静的事件可以单独补偿音量；Writing 和 Glitch 具备真正的时间轴循环能力，并且仍能在正确时机停止和释放；导出包也明确包含运行时必需的 Bank 与 GUID 映射。MGR 运行时已经不再直接使用原来的五个 OGG 文件，不过 FMOD 工程 `Assets` 目录中的源 OGG 仍然是以后重新构建 `MGR.bank` 的素材，不能与运行时冗余副本一并误删。

验证结果：Release 配置编译和脚本打包均通过，未产生编译警告或错误；安装目录中的 DLL 与本地构建产物哈希一致，导出 PCK 中的 Bank 和 GUID 映射也已核对。最新实机反馈已经证明人物选择崩溃不再复现。循环事件的代码参数和 Bank 时间轴均已修正，后续如需继续微调，只需要修改集中音量参数或 FMOD 事件内容，不需要恢复旧的 OGG 运行时加载方案。

## 20260818-03 ✅

日期：20260818。

类型：BUG 修复。

修改内容：多人游戏中打出“活动小丑”并与抽牌堆中的诅咒交换位置时，房主以外的玩家可能因牌堆数据不同步而掉线。

原因定位：卡牌通过仅在发起手动打牌的本地客户端执行、且发生在同步 `PlayCardAction` 之前的 `OnEnqueuePlayVfx` 保存原手牌索引，随后又用该非同步索引直接重排真实手牌模型，导致不同客户端得到不同的手牌顺序。

修复与改善：移除展示阶段手牌索引对真实牌序的修改，让诅咒入手顺序完全由原生同步牌堆命令管理，同时保留活动小丑进入诅咒原抽牌堆位置的效果。

## 20260818-04 ✅

日期：20260818。

类型：BUG 预防与表现架构改进。

修改内容：演奏队列此前依靠根节点 `ZIndex = 55`、悬停 `300`、生成牌入队 `320`、离队 `450`、终结演出 `419/420` 以及独立 `CanvasLayer = 90` 保证可见。该方案能让队列高于人物，却也可能在选牌、查看卡牌或其他原版界面打开时继续覆盖在最上方，与已经出现的人物立绘遮挡问题属于同类层级风险。

原因定位：这些层级值把“队列内部谁盖住谁”和“整个队列相对原版 UI 位于哪里”混在同一套全局 Z 排序中。只把根节点从正 Z 改为零仍不够，因为卡牌子节点、悬停大图、入队和离队动画的更高相对 Z 仍可能越过原版 UI；直接删除全部 Z 又会破坏重叠卡牌顺序、悬停置顶、五线谱前后关系和金枪鱼冲锋演出。

修复与改善：将持久队列、命中代理和临时卡面统一迁入原版战斗 UI 的自由定位容器 `MessyCardPreviewContainer`，新增轻量跟随节点，把人物画布变换和队列偏移转换到战斗 UI 坐标，因此队列仍会跟随人物移动、缩放和震动。队列内部拆分为五线谱、卡牌、终结撞击和终结卡影四个固定宿主；普通卡牌通过 `MoveChild` 按机械顺序排列，悬停与离队时临时移动到末尾，状态刷新后重新应用该临时顺序。悬停大图、生成牌入队和真实出牌入队均恢复默认 Z，并显式完成视口到父节点局部坐标的转换；临时 `NCard` 进入和离开 MGR 流程时都会复位对象池层级状态。

界面保护同时补上了 `InspectCardScreen`，并直接订阅卡牌查看与遗物查看界面的可见性变化。选牌 overlay、capstone、地图、卡牌查看或遗物查看出现时，持久队列、透明命中区域和悬停大图会一起隐藏。演奏队列相关代码目前不再使用任何非零 Z 或私有高 CanvasLayer，前后关系完全由原版战斗 UI 分支和节点树顺序决定。

后续扩展：音符栏原先仍保留根节点 `ZIndex = 50`、命中区域 `20`、提示框绝对 `4096` 以及内部负 Z，因此进一步迁入同一套原版战斗 UI 分层。共享宿主固定按“音符栏 → 演奏环境 → 演奏卡牌”绘制，保证二者重叠时演奏堆覆盖音符栏；音符星屑、空槽、音符本体和命中区域则改用槽位树顺序。白虎野之女的持续萤火也移除 `ZIndex = 18` 并进入演奏环境层。完成这些修改后，专门抬高所有提示框和药水弹窗的两个全局补丁已删除，相关 UI 恢复原版层级。

验证结果：已完成非零 Z、旧层级参数、旧 `CanvasLayer` 和对象池复位点的静态检索。此前出现的 `CardLocation` 和伤害修改接口错误，后来确认是项目默认 v0.111 兼容目标与本地正式版 v0.107 游戏程序集不匹配，并非代码基线损坏。明确使用 `Sts2CompatTarget=0.107.1` 后，完整 Release 构建为 0 警告、0 错误，PCK 导出与本地游戏目录安装均已完成；本地构建 DLL 与安装目录 DLL 的 SHA-256 完全一致。作者于 2026-08-19 确认相关实机回归通过，本项已验证并发布。

## 20260818-05 ✅

日期：20260818。

类型：性能优化与功能改进。

修改内容：部分设备运行 MGR 的逐帧人物动画与角色周围常驻特效时可能出现掉帧，但玩家此前无法按需关闭这些纯视觉内容。

原因定位：人物战斗场景会固定创建一百帧循环动画状态机、环境星光、光粒、能力光环、人物虚影与呼吸地影，且没有持久化的本机性能开关。

修复与改善：在模组选项页新增全局保存的“关闭人物动画”开关，开启后同时使用静态人物首帧并跳过角色周围常驻特效的初始化、更新与绘制。

## 20260818-09 ✅

日期：20260818。

类型：项目架构与开发流程改进。

修改内容：MGR 需要同时支持当前正式版 v0.107.1 和最新测试版 v0.111，但不希望依赖 CVC，也不希望以后为所有过期中间测试版无限累积 DLL。

原因定位：单一新版 DLL 无法可靠跨越卡牌去向、伤害 Hook、复制、抽牌、选择上下文和 VFX 等 ABI 变化；直接内嵌通用重写器又会引入 Mono.Cecil、加载顺序、profile 维护和运行时诊断成本。后续维护如果缺少固定归档和滚动规则，还容易在 Steam 更新后失去旧版原始程序集。

修复与改善：完成稳定 `MGRMod.Loader`、正式版/测试版两个原生 payload、SHA-256 版本清单、条件编译接口分支、集中反射桥和双版本构建链；保存真实 v0.107.1 原始引用并验证可独立重建。发布策略固定为只滚动保留“当前正式版 + 最新测试版”，同时新增 [`MGR双版本兼容维护手册.md`](../project/MGR双版本兼容维护手册.md)，集中记录发布结构、CVC 取舍、源码分支边界、DLL 归档、未来升级流程和验证基线。

后续构建流程改进：真实 v0.111.0 原始引用也已连同版本信息和 SHA-256 固定归档。`MGRMod.csproj` 默认在每次普通本地构建中导出一次共享 PCK，隔离编译 v0.111.0 与 v0.107.1 payload，构建稳定启动器并生成 manifest；内层构建带递归保护。默认结果直接刷新本地游戏目录中的完整六文件包，设置 `CopyModOnBuild=false` 时则生成到项目 `.artifacts` 而不部署。工坊准备脚本复用同一条构建链，不再先构建单版本 DLL、再重复调用双版本脚本。这样本机 Steam 分支切换不会再让 `Sts2CompatTarget` 与实际引用错配，也不会把单个 v0.107 payload 误装成顶层 `MGRMod.dll`。

v0.107.1 实机启动修复：首次切回正式版验证时，启动器虽正确选择 107 payload，但旧版 `Mod` 只提供单数 `assembly` 字段且没有新版公开关联方法；原回退逻辑只识别复数 `assemblies`，导致 payload 没有进入原版 `ModelDb` 扫描并在角色模型预热阶段报错。第一次补充单数字段后实机错误仍在，进一步反编译确认 `TryLoadMod` 会在初始化器返回之后把 `mod.assembly` 再次覆盖为顶层 Loader，因此问题不是“字段写不进去”，而是“写入时机早于原版最终赋值”。启动器现改为在旧版 `OnModDetected` 完成通知中最终恢复 payload，同时保留新版公开方法、复数列表路径和明确失败检查，避免再次静默演变为模型缺失异常。

验证结果：无额外参数的 `dotnet build MGRMod.csproj -c Release` 全链构建为 0 警告、0 错误；本地游戏目录包含完整双版本包，顶层身份为 `MGRMod.Loader`，两个 payload 身份为 `MGRMod`，manifest 的两项哈希均与实际文件一致。真实程序集反射确认 v0.107.1 的单数 `assembly` 可写，v0.111.0 则提供公开关联方法和复数 `assemblies`；使用真实 v0.107.1 类型模拟“原版覆盖为 Loader → `OnModDetected` → 恢复 payload”后最终关联正确，启动器使用两套固定真实引用分别编译均为 0 警告、0 错误。作者于 2026-08-19 确认正式版与测试版实机回归通过，本项已验证并发布。

## 20260818-10 ✅

日期：20260818。

类型：BUG 修复。

修改内容：“想象、创造”从已被感染棱柱污染的卡牌生成类型候选时，候选副本会继承原卡的污染；即使随后改成非技能牌，打出时仍会触发污染。

原因定位：原版卡牌深拷贝会连同 `Affliction` 一起复制，而污染只在首次附加时检查卡牌类型，不会在类型改变后自动失效。“想象、创造”先克隆卡牌、再覆盖候选类型，因此复制出的卡牌继续携带污染。

修复与改善：每个类型候选在克隆完成后、覆盖目标类型前检查并清除 `Tainted`。该处理对攻击、技能、能力、诅咒和状态候选一致生效，只清除候选副本的污染，不修改原卡、不清除其他异常，也不改变感染棱柱或其他复制来源的全局规则。

验证结果：使用固定的 v0.107.1 与 v0.111.0 原版引用分别完成 Release 编译，两次均为 0 警告、0 错误；静态检查确认处理顺序为“克隆候选 → 清除污染 → 覆盖目标类型”，内容登记校验通过。作者于 2026-08-19 确认五种候选与最终变形卡的实机行为通过，本项已验证并发布。

## 20260818-11 ✅

日期：20260818。

类型：数值平衡。

修改内容：“鮪鱼烧”由商店遗物调整为 `Uncommon`（罕见）遗物，休息处锻造后的回复由最大生命的 15% 改为固定 12 点生命。

原因与改善：“鮪鱼烧”改入 `Uncommon` 掉落层级，并以固定回复降低高最大生命时的收益膨胀。此前把“罕见”误改成了 `Rare`（稀有）；作者已确认实际设计为 `Uncommon`（罕见），本次据此回滚相关工坊表述。

同步范围：实现、十种语言的遗物描述和内容登记同时更新；遗物的效果触发时点不变。

验证结果：当前源码确认 `Rarity` 返回 `Uncommon`、动态值为 `HealVar(12)`，锻造回调按固定值回复；内容登记校验及 v0.107.1、v0.111.0 双版本 Release 构建均为 0 警告、0 错误，最新内容包已经上传。十种 Steam 工坊简介同步使用各语言原版 `Uncommon Relic` 术语。

## 20260818-12 ✅

日期：20260818。

类型：数值平衡与功能改进。

修改内容：“注视”的基础伤害提高 1 点，并将原本根据目标负面状态种类获得格挡的效果改为每种抽 1 张牌。

修复与改善：战斗内卡牌描述会根据当前目标实时显示总抽牌数，便于玩家在出牌前确认收益；十种游戏内语言文本已同步更新。

验证结果：内容校验、十种语言 JSON 解析和 Release 编译均通过，且已经发布到 Steam 创意工坊。

## 20260818-13 ✅

日期：20260818。

类型：BUG 修复。

修改内容：修复了时间线之东被降级后丢失永久成长音符的 BUG。

原因定位：原版降级会把战斗卡牌的整套动态变量替换为数据库基础卡的变量，再调用卡牌的 `AfterDowngraded()`。可序列化的 `CurrentNotes` 永久值和牌组原卡始终正确，但战斗副本没有在该回调中把 `Notes` 从永久值重新同步，因此只在当前战斗表现为永久成长丢失。

修复与改善：为“时间线之东”实现降级回调，在原版完成基础变量替换后仅恢复 `Notes = CurrentNotes`。降级仍会正常撤销普通升级，使以后每次满足条件时的永久增量从升级值 2 暂时恢复为基础值 1；MagiKnight 消失后原版重新升级流程也不会重复修改已经积累的音符总量。

验证结果：内容登记校验通过；使用固定的 v0.107.1 与 v0.111.0 原版引用分别完成 Release 编译，两次均为 0 警告、0 错误。静态核对确认原版调用顺序为“替换基础动态变量 → `AfterDowngraded()` → 重新应用附魔与异常”，且新增回调只写回 `Notes`。作者于 2026-08-19 确认完整降级与恢复流程通过，本项已验证并发布。

## 20260819-01 ✅

日期：20260819。

类型：本地化与表现改进。

修改内容：审校“卡农形态”的十语言卡牌与同名能力文本。法语标题由容易被理解为“规范形式”的 `Forme canonique` 改为音乐语义明确的 `Forme en canon`；日语、韩语和俄语标题分别统一为更符合原版形态牌命名方式的 `カノン化`、`카논의 형상` 和 `Облик канона`。同时改善英语、德语和巴葡的生效时点句，以及日语、韩语和西班牙语的下次打出预览标签。

范围说明：本次只修正误译、标题体系和自然度；不修改卡牌或能力的玩法代码，也不在任何语言中追加“按顺序”说明。简中与繁中现有机制文本保持不变。

验证结果：十语言 JSON 与内容登记校验通过；卡牌和同名能力标题在每种语言中完全一致，能力文本中的 `Amount`、`HasCards`、`Cards` 占位符及 SmartFormat 结构全部保留。受影响文件通过差异格式检查，简中和繁中正文未改动。发布前仍需抽查卡面、能力提示和动态“下次打出”列表的实际换行。

## 20260819-02 ✅

日期：20260819。

类型：本地化与表述优化。

修改内容：重写“星空”关键词的十语言悬浮说明。旧文本只说明打出星空牌会生成星空音符、音符触发时获得能量，没有明确指出星空音符会替代该牌原卡牌类型对应的普通音符。

修复与改善：简中改为“生成星空音符而非原卡牌类型对应的音符。星空音符能给予能量”，能量继续用游戏内图标表示；繁中、英语、德语、法语、日语、韩语、巴葡、俄语和西班牙语按各自已经锁定的星空音符术语同步改写。只调整 `MGR_MOD_KEYWORD_STARRY.description`，不改变星空音符自身关键词、玩法代码或数值。

验证结果：十份 `card_keywords.json` 均可解析且 key 集合与简中一致；每个目标描述各含且仅含一个相同的能量图标资源，旧规则措辞已替换。本地化规范和九份语言指导已同步到 `2026-08-19-r15`，内容校验以 warnings-as-errors 通过。发布前仍需在游戏内抽查悬浮框换行和图标显示，因此本条尚未标记为已发布。

## 20260819-03 ✅

日期：20260819。

类型：BUG 修复。

修改内容：贴有演奏的原版“流星锤”会在下回合显示一张无法打出的手牌卡面，同时实际卡牌在演奏后进入弃牌堆。

原因定位：流星锤在抽牌前把上回合打出的同一卡牌实例从演奏使用的 `Play` 牌堆返回手牌；演奏队列仍保留该实例，并在稍后的回合开始钩子中跳过原生牌堆表现再次自动打出。模型最终进入弃牌堆，但刚创建的手牌节点没有参与这次无表现移动，因此留下不可交互的幽灵卡面。这不是原版创建了副本，而是演奏状态与原生牌堆同时声明同一实体牌的归属。

修复与改善：在每次原生牌堆移动完成后统一核对演奏实体归属。非结算中的队列牌只要离开 `Play`，原生目标牌堆取得所有权并自动解除演奏记录；正在结算的演奏条目继续由既有保护处理，普通演奏最终弃牌/消耗和活动小丑的队列换牌不受影响。修复不按卡名特判，因此同样覆盖采用相同回牌逻辑的“无休手斧”和未来其他主动移动演奏实体牌的效果。

验证结果：静态核对确认流星锤与无休手斧都在 `BeforeHandDraw` 使用原生 `CardPileCmd.Add(this, Hand)` 移动同一实例，新归属检查发生在该原生移动完成之后；内容校验通过。使用固定 v0.107.1 与 v0.111.0 原版引用分别完成 Release 编译，两次均为 0 警告、0 错误。仍需实机确认两张回牌卡的手牌交互和普通演奏牌的最终去向，因此本条尚未标记为已发布。

## 20260819-04 ✅

日期：20260819。

类型：BUG 修复与表现架构改进。

修改内容：“幽灵法则”挑选牌以及普通手牌显示时，少数卡牌可能随机获得异常高的显示优先级并遮挡其他卡牌或界面。

原因定位：问题不是未回收空节点挂在卡牌上，而是电子天使和黎明前线的表现流程分别把共享 `NCard` 临时提高到 `ZIndex = 340` 与 `120 + index`，随后直接归还原版对象池。原版对象池只复位常用变换与颜色，不复位 `ZIndex`、`ZAsRelative` 和 `ShowBehindParent`；幽灵法则虽然完全使用原版选牌接口，仍可能随机取得被污染的卡牌节点。

修复与改善：新增共享 `MgrCardNodePoolSafety`，让电子天使、黎明前线和演奏队列的所有 MGR 临时卡在归还对象池前统一恢复默认 Canvas 排序；原版延迟消耗动画也会在隐藏卡面后、实际回收前完成清洗。同时为 `NCard.OnReturnedFromPool()` 增加防御性后置复位，任何旧污染或遗漏状态在重新进入手牌、选牌网格、奖励与查看界面前都会被清除。补丁只作用于对象池生命周期边界，不改变卡牌模型、牌堆、结算或界面随后主动设置的正常层级。

验证结果：已静态核对所有 MGR 的 `NCard.Create`、高 Z 和释放位置；使用固定 v0.107.1 与 v0.111.0 原版引用分别完成 Release 编译，两次均为 0 警告、0 错误。尚需实机依次触发电子天使和黎明前线，再检查幽灵法则选牌、普通手牌、卡牌查看与奖励界面，因此本条尚未标记为已发布。

## 20260819-05 ✅

日期：20260819。

类型：BUG 修复与机制一致性改进。

修改内容：积雨云涂鸦让同一和弦额外触发时，金枪鱼横斩、金枪鱼重击和强袭仍只按音符槽完成次数计算伤害或费用；节拍器也只推进一次，和“每触发一次和弦”的卡牌及遗物描述不一致。

原因定位：战斗状态同时维护了“音符槽完成次数”和“和弦效果实际执行遍数”。积雨云、节拍器、吉他拨片与万象键宇宙走实际执行遍数，而三张金枪鱼牌及节拍器进度仍读取或更新完成次数，导致同一次重复在不同收益端采用不同口径。

修复与改善：玩法统一使用和弦效果实际执行遍数；每一遍同时累加本回合和本场战斗计数，金枪鱼系列及条件光效全部迁移到统一计数。基础和弦与积雨云等外部重复会推进节拍器，达到第7次时向当前和弦追加一遍；节拍器自己的追加遍仍计入其他玩法的和弦次数，但不会反向推进节拍器，保持与原版钢笔尖、双截棍一致的触发后归零逻辑。因此节拍器进度6与积雨云同时满足时会执行3遍，并在结束后只由积雨云重复推进到新一轮进度1。节拍器右下角正向显示已累计次数；累计到6时发光，真正触发时闪光并短暂显示完整阈值7，随后显示0或同组外部重复推进后的实际进度。

验证结果：v0.107.1 与 v0.111.0 固定引用下的隔离 Release 编译均为 0 警告、0 错误；旧完成次数玩法属性已移除，全部实际执行遍数仍进入统一回合/战斗计数。情景检查确认节拍器单独从进度6触发时执行2遍并最终归零；与积雨云同时满足时执行3遍，只有基础和弦与积雨云重复推进遗物，最终显示新一轮进度1。内容登记校验通过。仍需实机核对动画、卡面数字、费用、右下角计数和遗物发光，因此本条尚未标记为已发布。

## 20260819-06

日期：20260819。

类型：项目研究与视觉规范改进。

修改内容：将 RegentFX 本地参考资料从 v0.4.3 更新为线上 v0.5.1，重新恢复 PCK 与 DLL，并对新增的持续黑洞、局部屏幕扭曲、两段式攻击反馈、场景路径标记和长逐帧资源进行专项评估。

修复与改善：MGR 视觉规范新增持续/异步特效约束，明确生命周期版本号、重挂父节点时保留全局变换、共享材质复制后修改、基础循环与一次性强调分层，以及长逐帧资源只用于少数招牌演出的边界。结论记录于 [`RegentFX v0.5.1 资源与动画参考评估`](RegentFX-v0.5.1资源与动画参考评估.md)。

验证结果：线上源包、主参考镜像和恢复结果已用版本号、SHA-256、文件数及 GDRE 报告交叉校验；79 个 ILSpy C# 文件可读，1,211 个导入资源全部转换成功。本项没有修改 MGR 运行时代码或资源，不需要编译、实机和工坊内容发布，也不进入玩家更新说明。

## 20260819-07 ✅

日期：20260819。

类型：BUG 修复与文本一致性修复。

修改内容：心象蜃気楼由能力音符追加的固定格挡会在负强音把该音符抽牌量压到 0 时一并消失；同时能力的十语言静态说明与内容登记仍写每次 3 点格挡，而当前源码和卡牌动态说明实际为 4 点。

原因定位：追加格挡的实际数值始终直接读取 `MindMiragePower.Amount`，没有乘强音；问题来自音符结算在进入类型分支前统一检查强音修正后的效果量，效果量为 0 时直接返回，因此能力音符自己的抽牌和心象蜃気楼的独立固定反应被错误绑定。数值文本不一致则来自 2026-08-15 把源码从 3 调整为 4 后，静态能力说明和登记没有同步。

修复与改善：能力音符现在即使自身抽牌量为 0 也会继续进入能力音符分支；抽牌只在强音修正量大于 0 时执行，心象蜃気楼则始终按当前能力层数固定给予格挡。普通能力音符、万象音符复现及联机分享共用同一规则。十种语言的静态能力说明和内容登记同步为当前实际值 4；卡牌数值、能力层数、升级和能力音符的强音抽牌公式不变。

验证结果：静态情景核对确认负强音下为“抽 0、格挡 4”、0 强音下为“抽 1、格挡 4”、2 强音下为“抽 2、格挡 4”；万象与分享路径最终进入相同的能力音符处理。十语言旧值 3 已无残留，内容登记校验通过；v0.107.1 与 v0.111.0 固定引用的 Release 编译均为 0 警告、0 错误。仍需实机核对三档强音以及万象/联机分享的数值和动画，因此本条尚未标记为已发布。

## 20260819-08 ✅

日期：20260819。

类型：遗物表现改进。

修改内容：“你的舞台”在本回合一次性演奏加成尚未使用时持续发光；“吉他拨片”每遍和弦触发时不再播放完整粒子闪光或角色上方放大动画，只让遗物图标短暂微微发光。

实现说明：“你的舞台”在战斗开始及己方回合开始时切换为原版 `RelicStatus.Active`，首次成功给予演奏加成后恢复普通状态，并在战斗结束时清理发光；实际触发仍保留一次完整 `Flash()`。第一版“吉他拨片”只用 `ShouldFlashOnPlayer = false` 关闭角色上方三层放大图，但实机确认逐和弦 `Flash()` 仍会生成过大的遗物栏粒子；修正版移除该调用，改为约 0.24 秒的 `RelicStatus.Active` 微光脉冲。高频连续触发只延长当前微光，旧异步回调不能提前熄灭新脉冲，战斗结束会强制恢复普通状态。两项修改均不改变触发条件、结算次数、格挡值或演奏加成。

验证结果：第一版使用固定 v0.107.1 与 v0.111.0 原版引用分别完成 Release 编译，但作者实机确认“吉他拨片”反馈仍然过大；复查当前安装 payload 哈希后排除旧包，定位为 `Flash()` 本身仍在触发遗物栏完整粒子。修正版内容登记以 warnings-as-errors 校验通过，两套固定引用的 Release 编译均为 0 警告、0 错误；全项目检索确认和弦路径只调用 `PulseLightly()`，不再调用 `guitarPick.Flash()`。仍需部署修正版并实机确认微光观感，因此本条尚未标记为已发布。

## 20260819-09 ✅

日期：20260819。

类型：卡牌表现改进。

修改内容：为“心象蜃気楼”增加打出时横跨整个战斗画面的海浪翻滚演出，同时保留原有角色中心半透明蜃景和能力音符给予格挡时的镜像波纹。

实现说明：新增不依赖外部位图的屏幕采样 Shader，以由左向右移动的蓝青浪体、三道不规则浪脊、碎浪白沫、水面亮纹和局部画面折射形成约 1.12 秒的海浪。效果宿主插入 `GlobalUi.Overlays` 之前，使战场受到折射而正在展示的卡牌、选牌界面和弹窗保持清晰；联机只在出牌者本地生成。卡牌仅等待 0.18 秒开场节拍，海浪余段与原版能力施加反馈重叠，演奏重放时按既有视觉等待缩放，不改变能力数值、音符格挡或玩法顺序。

验证结果：内容登记以 warnings-as-errors 校验通过；v0.107.1、v0.111.0 payload 与稳定启动器均以 Release 配置构建成功，0 警告、0 错误，完整候选包生成于 `.artifacts/MindMirage-wave/`。第一次部署因已安装 DLL 被进程占用而安全停止，锁释放后已通过项目双版本脚本成功部署到本机游戏模组目录；安装目录两份 payload 哈希均与 manifest 一致，安装 PCK 与候选 PCK 的 SHA-256 同为 `CEB9A0EF46B481EFD656A1B9399A3E11200B103312F18BCEFDA549014DE05213`。仍需实机确认 Shader 编译、浪头节拍、卡面/UI 层级、演奏重放及不同宽高比分辨率观感，本条尚未标记为已发布。

## 20260819-10 ✅

日期：20260819。

类型：BUG 修复与结算性能优化。

修改内容：Bird 作为致死收尾时仍会先等待约 0.20 秒，再按伤害的一半逐个生成大量随机音符；这些音符还可能继续触发和弦与重复和弦，使胜负已定的战斗长时间不能结束。

原因定位：音符系统只对单个攻击音符检查是否还有可攻击目标，但通用音符生成入口、随机音符 RNG、同一和弦的逐音符循环、重复和弦循环及缩槽产生的多和弦循环都没有统一的战斗结束中止条件。Bird 自身的落地后等待也发生在任何敌方存活检查之前。

修复与改善：新增统一的音符序列终止判断，使用原版 `CombatManager.IsOverOrEnding`，并在战斗尚未正式切换状态时核对 `CombatState.Enemies` 是否确实全部 `IsDead`；不使用 `HittableEnemies == 0`，避免把仍存活但暂时不可命中的敌人误判为死亡。Bird 致死后直接跳过 0.20 秒等待和随机音符循环；其他来源在敌方清空后不再生成或复制音符、不再消耗随机音符 RNG。若攻击音符在和弦中杀死最后一名敌人，后续音符效果、联机分享、重复遍数和缩槽产生的后续和弦立即停止；已经完成的效果与计数不回滚。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用的 Release 编译均为 0 警告、0 错误。静态情景核对确认 Bird 致死发生在固定等待之前；普通/双倍/随机/复制音符均经过统一入口；和弦逐音符、万象分量、联机分享、重复遍数及缩槽多和弦均在每个可继续边界重新检查。攻击音符封锁仍只跳过攻击音符，不会把它误判为战斗结束并吞掉后续其他类型复制。仍需实机验证 Bird 单体致死、多敌人未清空、攻击音符致死、积雨云/节拍器待重复及暂时不可命中敌人场景，本条尚未标记为已发布。

## 20260819-11 ✅

日期：20260819。

类型：卡牌表现与视觉架构改进。

修改内容：为极彩色增加轻量局部棱镜扭曲，把银河灯改为“旧音符汇聚—转换—星空音符释放”，把陨星余波改为“碎片汇聚—玩法结算—向外余波”；同时为「 鳥 」增加不延长演出的短预兆，为立方棱镜增加每次出牌一次的光束折射。

实现说明：新增完全由 MGR 自主代码绘制和 Shader 构成的共享视觉组件。屏幕采样层插入原版全局 overlay 之前，卡牌、选牌与弹窗保持清晰；每个 Shader 实例使用独立材质并随短寿命宿主自动清理。银河灯只对当前音符取视觉快照，到达转换拍后仍调用原有移除和逐个生成接口；陨星余波的能量、抽牌和债务顺序不变。「 鳥 」的预兆完全复用既有鸟影寿命，立方棱镜的折射不按多段伤害重复生成。实现只借鉴预兆/结算分层和局部扭曲等动画结构，没有复制 RegentFX 的资源或源码。

验证结果：内容登记以 warnings-as-errors 校验通过；单版本 Release 编译及完整 v0.107.1、v0.111.0 双 payload 与稳定启动器构建均为 0 警告、0 错误，构建未部署到游戏目录。仍需实机核对 Shader 编译、屏幕位置、连续触发、银河灯不同音符数量、陨星余波结算拍及演奏重放观感，因此本条尚未标记为已发布。

## 20260819-12 ✅

日期：20260819。

类型：卡牌表现改进。

修改内容：完成蓝卡缺失动画首批梳理，为孤独宇宙、舞台预热、群青赞歌、全能、拖延症、甜甜圈洞与活动小丑补充各自的短促机制语义动画。

实现说明：新增独立 `MgrBlueCardVfx`，以代码绘制的孤星轨道、预热/释放弧、群青护盾谱线、起音/尾音双路、计时盘/药瓶星屑、收紧空洞护盾及卡牌交换轨迹表达七种机制。延迟演奏牌只在初次入队和最终兑现时强调，群青赞歌等重复牌使用轻量且按系列合并的反馈；全能和活动小丑只在对应分支确实成立时播放。电子天使、彩虹音阶、宇宙香料与星降之海经共享视觉文件复查后确认已有反馈，没有重复叠加。全部新增节点非阻塞并自动释放，不改变数值、费用、牌堆、演奏、存档或联机协议。

验证结果：内容登记以 warnings-as-errors 校验通过；v0.107.1 与 v0.111.0 固定原版引用下的 Release 编译均为 0 警告、0 错误。静态核对确认七张牌的视觉调用均位于既有成功分支，未增加玩法等待；仍需实机检查不同演奏次数、Replay、最终兑现、活动小丑三种交换路径及无有效条件时不误播，因此本条尚未标记为已发布。

## 20260819-13 ✅

日期：20260819。

类型：遗物数值平衡。

修改内容：原版遗物“棋子”会在每次打出能力牌时抽 1 张牌，与 MGR 的固有能力重复，因此不再出现在 MGR 的随机遗物奖励池中。

实现说明：在只对 `MgrCharacter` 生效的 `RelicGrabBag` 过滤补丁中新增 `GamePiece` 排除项；MGR 当前明确排除奥利哈钢、怀表和棋子。其他角色的遗物池不变，事件、调试或其他直接授予遗物且不经过该随机奖励袋的来源也不受影响。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用的 Release 编译均为 0 警告、0 错误。仍需实机确认 MGR 随机遗物奖励不会再抽到棋子，因此本条尚未标记为已发布。

## 20260820-01 ✅

日期：20260820。

类型：BUG 修复。

修改内容：“想象、创造”选择“疯狂科学”并生成不同卡牌类型候选时会抛出异常，导致选择流程无法正常结束，表现为界面或战斗直接卡住。

原因定位：“想象、创造”通过修改 `CardModel.Type` 的基类自动属性字段实现战斗内类型替换，但“疯狂科学”自行重写了 `Type` getter，并始终从事件配置字段 `TinkerTimeType` 返回原类型。创建第一个不同类型候选后，覆盖自检因此失败并中断整段异步选牌流程。

修复与改善：为 `MadScience.get_Type` 增加仅在存在 MGR 战斗内类型覆盖时生效的后置处理，使候选与最终变形卡返回攻击、技能、能力、诅咒或状态中的所选类型；“疯狂科学”的 `TinkerTimeType` 不被改写，因此原本的目标方式、伤害/格挡/能力效果与事件附加词条保持不变。作者首次实机复测后确认第一版仍会卡住，日志证明补丁类虽然进入程序集，却没有在 `Entry.Initialize()` 中登记，运行时实际从未应用。修正版已补上该登记，并同时补齐同样遗漏的类型克隆保持与 MGR 遗物奖励过滤补丁。普通卡牌及没有经过“想象、创造”修改的“疯狂科学”不受影响。

验证结果：第一版实机日志在选择 `MAD_SCIENCE` 后明确报错 `Failed to change card MAD_SCIENCE type to Attack`，启动阶段只应用原有 13 个 MGR runtime patch 目标，推翻了此前仅凭补丁类和 getter 反射所得的通过判断。修正版内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用的 Release 编译均为 0 警告、0 错误。对生成 DLL 的 IL 检查确认 `Entry.Initialize()` 已包含动态类型覆盖、类型克隆保持和遗物奖励过滤三项 `RegisterPatch<T>()` 调用，预期 runtime patch 目标总数由 13 增至 16。仍需重新打包部署并以启动日志和五种候选实机复测确认，因此本条尚未标记为已发布。

## 20260820-02 ✅

日期：20260820。

类型：BUG 修复。

问题表现：MGR 进入“假商人”定制事件时会显示为战士，离开后在下一场普通战斗黑屏。

原因定位：当前存档中的角色 ID 仍为 MGR，事件没有真正改写角色。原版“假商人”使用独立布局，并在 `NFakeMerchant.AfterRoomIsLoaded()` 中临时创建角色战斗形象；随后无条件对该形象请求 Spine 专用的 `relaxed_loop`。MGR 的角色形象是普通 Godot 节点而非 Spine。进一步审计确认 `SpineAnimationAccess` 在没有 Spine 时会安全无操作，因此这句调用本身不会直接抛错；当前能够确定的是假商人这个特殊场景的形象选择/兼容分支曾回退成战士占位，黑屏与该临时形象初始化链路相关，但缺少事发会话日志，不能把后续房间状态异常进一步断言为某一条 Spine API 抛错。

修复与改善：新增最高优先级的 `MgrFakeMerchantCharacterVisualPatch`，只在假商人的玩家列表恰好包含一名 MGR 时接管 `AfterRoomIsLoaded()`。补丁直接通过 MGR 自有角色场景创建非 Spine 形象，保持原版单人位置和欢迎对白，并在初始化完成后跳过原版 Spine 专用动画调用；创建失败会记录警告并退回既有路径。其他角色、意外的多人组合、普通事件和普通战斗均不受影响。角色场景创建集中到 `MgrCharacter.CreateStaticCreatureVisuals()`，普通战斗的既有创建入口也复用同一工厂，避免两条路径的资源地址漂移。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用的 Release 编译均为 0 警告、0 错误。Mono.Cecil 静态核对确认两版原版 DLL 均存在签名一致的 `AfterRoomIsLoaded()` 与 `StartCharacterAnimation(NCreatureVisuals)`，最终 MGR 程序集的 `Entry.Initialize()` 确实包含新补丁注册，排除了“补丁类存在但未应用”的遗漏。追加的两版全量调用点审计确认，直接读取 `NCreatureVisuals.SpineAnimation` 的角色相关外部场景只有假商人、游戏结束和时间线角色解锁：前者已由 MGR 接管，游戏结束已有 RitsuLib 0.5.13 专门兼容补丁，角色解锁对 `RequiresEpochAndTimeline = false` 的 MGR 不可达；普通战斗、商店和休息处已有 Spine 判定或 RitsuLib 非 Spine 状态机/世界场景接管。没有新增全局 Spine API 补丁：空值安全包装到调用时已失去所属角色身份，强行在该层替代既无法可靠识别 MGR，也可能误伤其他非 Spine 模组角色。当前 runtime patch 目标预期由 16 增至 17。仍需部署后实机验证假商人购买/离开、恶臭药水特殊战斗及随后普通战斗，因此本条尚未标记为已发布。

## 20260820-03 ✅

日期：20260820。

类型：卡牌表现改进。

修改内容：为“光线歌”增加从人物位置向全屏持续放射、顺时针旋转的专属光束，使特效不仅在打出瞬间出现，还能覆盖这张牌完整的连续选牌阶段。

实现说明：新增不依赖外部贴图的 `MgrLightSongVfx`。打出时从人物中心展开八道黄白、淡蓝与淡紫渐变光束，光束越接近屏幕边缘越宽，并让短促亮光包沿光束持续向外传播；人物周围同时保留双层旋转光环和轻量星芒。视觉由 `LightSong.OnPlay()` 持有的独立租约管理，从第一个选牌步骤开始维持到第三个选牌步骤结束；正常选完、取消或异常退出都会进入统一淡出，旧租约只能清理自己创建的节点，不会误删后来播放的实例。第一版把节点放在 `CombatVfxContainer` 并设置正 `ZIndex=31`，静态判断其会低于选择界面；作者实机确认该判断错误，光束实际会覆盖手牌、抽牌堆和弃牌堆中的部分候选卡面。修正版改用插在 `GlobalUi.Overlays` 之前的专用普通 underlay，并把局部 Z 恢复为 0，使完整原版选择分支按树顺序稳定覆盖光束；淡出结束时同时释放 underlay，避免残留空节点。效果仍只为出牌者本地生成，不参与牌堆状态或联机同步。

验证结果：第一版虽然通过内容校验与双版本静态编译，但作者实机确认三类选择卡面均可能被光束盖住，因此原“候选卡牌位于其上方”的验证结论已经撤销。修正版内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用的 Release 编译均为 0 警告、0 错误，稳定启动器同时构建成功，构建未部署到游戏目录。静态核对确认光束已脱离战斗前景特效容器，underlay 位于 `GlobalUi.Overlays` 之前且不再使用正 Z。仍需实机重新确认手牌、抽牌堆、弃牌堆三类选择卡面、人物发射点、不同宽高比与结束回收，因此本条尚未标记为已发布。

追加层级诊断及更正（2026-08-21）：第一版 `CombatVfxContainer(-9) + local Z 31` 确实会让光束直接盖住部分选择卡面；第二版的 `GlobalUi.Overlays` 前中性 Z underlay 能位于抽牌堆、弃牌堆 Overlay 下方，却仍可能绘制在 `RoomContainer/CombatUi` 的手牌选择上方。这是纯粹的光束遮挡问题，建议下一版把光束放回 `CombatVfxContainer` 并保持局部 Z 为0。玩家随后明确说明其所称“图层错位”是人物出现在部分卡牌上方、部分手牌彼此排序异常，而非光束盖住卡面；因此此前把玩家报告等同于第一版光束层级问题的结论撤回。重新审计确认光线歌没有修改原生卡牌、Holder 或人物的任何 CanvasItem 排序字段和树顺序，原版手牌选择对 Hand 的临时置顶与索引恢复在 v0.107.1、v0.111.0 中也成对且一致。真实卡牌错层更符合 `20260819-04` 已定位的共享 `NCard` 对象池层级污染：光线歌连续打开手牌、抽牌堆和弃牌堆选择，只是高概率取得并显示被污染的池节点。当前工程已经注册 `MgrNCardPoolLayerResetPatch`，在两版均存在的唯一 `NCard.OnReturnedFromPool()` 后把 `ZIndex`、`ZAsRelative` 与 `ShowBehindParent` 恢复默认；静态上应能阻断该问题，但仍需沿光线歌三步选择做实机回归后才能宣称完全修复。

## 20260820-04 ✅

日期：20260820。

类型：卡牌表现改进。

修改内容：为“百鬼夜行”增加伤害前的全屏暗幕与轻微空间扭曲，并把原有敌人黑焰命中扩展为黑色波纹、墨焰和近黑火焰同时爆发的结算拍。

实现说明：新增不依赖外部贴图的 `MgrHyakkiYagyoVfx`。卡牌存在可命中敌人时，先在 `GlobalUi.Overlays` 下方插入屏幕采样 Shader：画面先压暗至最高约 40%，随后逐渐出现低振幅的破碎环形折射；约 0.36 秒到达冲击拍后才开始既有全体伤害。每个命中节点以三层错相、带缺口的近黑波纹和酒红边缘表达“百鬼扩散”，同时保留原生近黑酒红火焰，并叠加代码绘制的墨焰轮廓。全屏效果余下尾段与命中重叠并自行释放，不再增加结算后的等待；演奏重放通过 `GetVisualWaitDuration` 压缩前奏，联机只在出牌者本地生成全屏层。伤害数值、目标、每名敌人的诅咒音符、随机诅咒及其先后顺序保持不变。

验证结果：内容登记校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为 0 警告、0 错误。静态核对确认视觉等待只位于伤害指令之前，命中组合节点和全屏节点均有受控寿命。仍需实机确认 Shader 编译、深色战斗背景可读性、单体与多体敌人落点、手动/演奏打出、联机本地显示、不同宽高比与视觉结束后无残留，因此本条尚未标记为已发布。

## 20260820-05 ✅

日期：20260820。

类型：卡牌表现改进。

修改内容：为“热异常”成功触发伤害翻倍时增加自屏幕下方向上方推进的热浪前置动画；没有翻倍的普通出牌继续直接播放原有敌人火焰命中与伤害。

实现说明：新增不依赖外部贴图的 `MgrHeatAbnormalVfx`。出牌时先保存成长前的基础伤害，执行既有乐句起始成长后再比较基础值；只有伤害确实提高才在 `GlobalUi.Overlays` 下方插入屏幕采样 Shader，以不规则上升浪头、宽而透明的热空气尾迹、橙红薄光和局部折射模拟热浪。约 0.72 秒浪头接近屏幕上沿时开始原有全体攻击，视觉余段与每名敌人的火焰爆发重叠并自行释放。热浪明显程度沿用对数伤害缩放思路，以翻倍后的自身伤害为输入并限制在 0.72 至 1.34 倍；多次成长能够逐步增强，但不会无限遮满战场。普通出牌没有新增节点或等待，原有伤害值、目标和火焰命中参数均未修改；演奏重放仍由统一视觉等待倍率压缩前奏，联机只在出牌者本地生成屏幕效果。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为 0 警告、0 错误。静态核对确认成长成功判断位于既有卡牌变更之后，热浪等待位于 `DamageCmd` 之前，普通分支直接进入原攻击流程。仍需实机确认 Shader 编译、从下向上的运动方向、翻倍前后分支、不同伤害等级、演奏重放、联机本地显示、非 16:9 分辨率和节点清理，因此本条尚未标记为已发布。

## 20260820-06 ✅

日期：20260820。

类型：卡牌表现改进。

修改内容：为“轩辕十四”增加攻击前的七彩极光汇聚演出；多股极光丝缕先沿弧线与螺旋轨迹汇聚到人物，汇聚完成后才开始原有十四段攻击。

实现说明：新增不依赖外部贴图的 `MgrRegulusVfx`，每次打出只创建一次九股曲线极光，不随十四段伤害重复铺设。每股丝缕使用不同的起点、椭圆半径、正反旋转方向、弯曲和轻微波动，外层为低透明柔光、内层为连续变色的白亮细芯；末段在人物中心收紧为三圈彩色弧与星芒。手动打出会等待约 0.68 秒至所有丝缕到达人物，再执行既有 `DamageCmd`；剩余约 0.20 秒余辉与首轮命中重叠并自行释放。演奏重放只压缩这段纯视觉前奏，原有伤害、段数、目标、枪击、星光命中、音符声与回手减费均未修改。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为 0 警告、0 错误。静态核对确认唯一显式等待位于 `DamageCmd` 之前，极光根节点每次出牌只创建一次且到期自行释放，原有十四段命中节点仍由同一个伤害命令负责。仍需实机检查曲线可读性、人物落点、手动/演奏时序、十四段性能、联机本地显示与节点清理，因此本条尚未标记为已发布。

## 20260820-07（已撤销）

日期：20260820。

类型：卡牌数值平衡。

修改内容：花葬基础伤害由6（8）调整为5（7）；塞壬之歌的升级格挡增量由3调整为2，因此格挡由6（9）调整为6（8）；卡农形态升级不再将3费降为2费，改为获得保留。

实现说明：花葬保留原有+2伤害升级和按未被格挡伤害生成攻击音符的规则；塞壬之歌只降低升级提供的额外格挡，不改变基础格挡、演奏、群体力量削减或消耗。卡农形态升级通过原生 `CardKeyword.Retain` 获得实际保留机制，费用保持3；MGR 描述排序层将其自动生成的保留关键词移至第一行。简中、英语、德语、法语、日语、韩语、巴葡、俄语、西班牙西语和繁中的显示均直接复用对应原版关键词译文，正文 JSON 不手写关键词，因此不会出现重复文本或翻译漂移。

验证结果：十语言卡农形态正文均存在且非空，并逐一核对对应原版 `RETAIN.title`；正文未重复硬编码保留关键词。内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为0警告、0错误。仍需实机确认三张牌的基础/升级预览和实际结算，尤其是卡农形态升级后保持3费、跨回合保留且保留提示位于第一行；本条尚未发布，不添加勾选。

## 20260820-08 ✅

日期：20260820。

类型：卡牌表现改进。

修改内容：再次增加获得星空音符时从屏幕上方坠落的小星星数量，由每个实际生成的星空音符随机 5 至 8 颗提高到 8 至 12 颗。

实现说明：只调整 `MgrVisualTuning.StarryNoteVfx` 的集中数量上下界，统一入口 `MgrStarryNoteVfx` 仍按每个实际生成的星空音符随机取值并创建短寿命星星。星星的散布范围、尺寸、下落速度、寿命、层级和仅本地显示条件均不变；不新增专属来源分支，也不改变星空音符数量、能量、玩法随机数或联机协议。视觉登记中残留的旧 2 至 4 颗说明同时修正为 8 至 12 颗。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为 0 警告、0 错误。静态核对确认统一生成入口继续使用集中上下界，未改变其他视觉参数或星空音符玩法流程。仍需实机检查连续生成和大量生成时的密度、遮挡与帧率，因此本条尚未标记为已发布。

## 20260820-10（已撤销）

日期：20260820。

类型：卡牌数值与机制平衡。

修改内容：“轮回”由基础1费、升级移除虚无，调整为基础2费、升级后1费；基础与升级版本均彻底移除虚无，不再与虚无机制产生任何关系。

实现说明：卡牌构造费用由1改为2，删除 `CanonicalKeywords` 中唯一的 `CardKeyword.Ethereal` 声明，并把升级逻辑从移除虚无改为费用降低1。轮回的十语言正文原本只描述“攻击音符被和弦消耗时获得活力”，没有手写虚无；虚无文本和右侧提示完全由原版关键词系统生成，因此删除源码关键词后十种语言会一起停止显示，无需复制或改写翻译。能力层数、活力值、攻击音符触发条件、重复和弦计算与既有视觉反馈均未修改。

验证结果：内容登记摘要已同步为2（1）费且不再提及虚无，并以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为0警告、0错误，稳定启动器同时构建成功。静态检索确认 `Samsara.cs` 不再包含 `CardKeyword.Ethereal` 或虚无移除逻辑。仍需实机核对基础/升级费用、升级预览、关键词悬浮提示及回合结束时未打出的轮回不会再因虚无被消耗，因此本条尚未标记为已发布。

## 20260820-09 ✅

日期：20260820。

类型：卡牌表现改进。

修改内容：为“塞壬之歌”增加从画面上方向下方推进的轻量沉没波纹与空间折射；开场波纹建立后，人物获得格挡和敌人降低力量会在动画尾段中结算，不再因等待完整动画而拖慢首次进入演奏队列。

实现说明：新增不依赖外部贴图的 `MgrSongOfSirenVfx`，在 `GlobalUi.Overlays` 下方通过屏幕采样 Shader 绘制自上而下的三层浅蓝紫波纹、低幅横向扰动和向下沉降的画面折射。第一版手动打出时完整等待约0.70秒，再按原相对顺序获得格挡、在每名敌人身上播放既有小型声浪并施加力量降低；由于 MGR 还会在 `AfterCardPlayed` 中等待技能音符，演奏队列只能在这些流程结束后安全接管真实卡牌节点，导致首次入队明显迟缓。修正版保留0.70秒完整 Shader 寿命，只阻塞0.20秒开场节拍，然后让格挡和力量降低与剩余0.50秒尾段重叠。演奏重放会分别对视觉寿命和开场等待应用相同的既有序列倍率；没有提前移动真实卡牌，也没有改变格挡值、力量降低值、演奏次数、技能音符、消耗与目标。

验证结果：第一版通过静态校验后，作者实机确认完整视觉等待会使首次进入演奏队列体感过慢。修正版内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为0警告、0错误，稳定启动器同时构建成功。静态核对确认 Shader 节点仍使用完整压缩寿命，玩法只等待压缩后的0.20秒开场节拍，既有出牌结果路由与 `Played` 后入队安全边界未改。仍需实机确认入队提速、Shader 编译、自上而下方向、尾段数值反馈、两次演奏重放、联机本地显示、不同宽高比和节点清理，因此本条尚未标记为已发布。

## 20260820-11 ✅

日期：20260820。

类型：卡牌数值与机制平衡。

修改内容：“星辰导引”的升级伤害增量由4点降低为2点，实际伤害从10（14）调整为10（12）；升级后的星辰导引现在会将生成的随机星空牌完整升级后再加入演奏队列。

实现说明：保留10点基础伤害，只将 `OnUpgrade()` 的伤害增量改为2。随机候选、普通星空牌权重、卫星少女2/3相对权重和轩辕十四1/3相对权重均不变；从候选创建战斗实例后，若来源星辰导引已经升级，则复用“游生梦死”音符还原工厂使用的原生 `CardCmd.Upgrade(card, CardPreviewStyle.None)` 路径，再执行既有中央展示和演奏入队。因此生成牌并非只显示绿色文字，而会真正获得该牌的费用、数值、关键词和机制升级。普通版仍生成未升级牌。简中、繁中、英语、德语、法语、日语、韩语、巴葡、俄语与西班牙西语都使用 `IfUpgraded` 条件说明升级牌分支；简中源快照哈希及十份指导版本同步更新至 `2026-08-20-r16`。

验证结果：十份 `cards.json` 均可解析，星辰导引描述各含且仅含一个 `Damage` 与一个 `IfUpgraded` 格式器；内容登记以 warnings-as-errors 校验通过。第一次 v0.107.1 编译发现缺少 `CardPreviewStyle` 的原版 UI 命名空间，引入与音符还原工厂相同的 `MegaCrit.Sts2.Core.Nodes.CommonUi` 后，固定 v0.107.1 与 v0.111.0 原版引用的 Release 编译均为0警告、0错误。仍需实机核对基础/升级数值、十语言条件文本、生成牌升级标记和完整升级效果，因此本条尚未标记为已发布。

## 20260820-12（已撤销）

日期：20260820。

类型：卡牌数值平衡。

修改内容：撤回卡农形态“升级后保持3费并获得保留”的调整，恢复为普通版3费、升级版2费，升级前后均不具有保留。

实现说明：卡牌升级重新使用 `EnergyCost.UpgradeBy(-1)`，不再添加 `CardKeyword.Retain`；描述排序层和文本规范中仅为卡农形态保留关键词增加的特殊分支一并移除。能力效果、从下一回合开始生效、叠加层数、复制顺序、动态预览和人物背后的时间轮特效均保持不变。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为0警告、0错误，且明确关闭PCK导出与游戏目录复制。静态核对确认升级只执行 `EnergyCost.UpgradeBy(-1)`，卡农形态已从保留关键词置顶分支中移除，登记摘要同步为3（2）费。仍需实机核对基础/升级预览和实际费用；本条尚未发布，不添加勾选。

## 20260820-13（已撤销）

日期：20260820。

类型：卡牌数值平衡。

修改内容：作者在上一轮3（2）费回滚后重新确认最终方案：卡农形态普通版与升级版均为3费，升级后获得保留。

实现说明：本条覆盖同日 `20260820-12` 的临时回滚结论。升级重新添加原生 `CardKeyword.Retain`，不再降低耗能；描述排序层仅对升级后实际存在的保留关键词执行第一行置顶，十语言继续复用原版保留译文，不在正文中重复手写。能力效果、从下一回合开始生效、层数叠加、复制顺序、动态预览和时间轮特效均保持不变。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为0警告、0错误，且明确关闭PCK导出与游戏目录复制。静态核对确认升级只添加 `CardKeyword.Retain`、不再修改耗能，卡农形态已重新进入保留关键词置顶分支，登记摘要同步为3费且升级获得保留。仍需实机核对升级预览、实际费用和保留位置；本条尚未发布，不添加勾选。

## 20260820-14 ✅

日期：20260820。

类型：卡牌数值平衡。

修改内容：依据1376局有效单人对局的分进阶遥测结论，卡农形态最终恢复为普通版3费、升级版2费，升级前后均不具有保留。

实现说明：本条覆盖同日 `20260820-13` 的3费保留实验方案。升级重新使用 `EnergyCost.UpgradeBy(-1)`，移除原生保留关键词；描述排序层中仅为卡农形态服务的保留置顶分支同步删除，内容登记恢复为3（2）费。能力效果、从下一回合开始生效、层数叠加、复制顺序、动态预览和时间轮特效均不改变。该决定依据总体“高选取、中游胜率”、A0偏弱及A10样本较小且同局对照无明显增益的结果，不额外加强到基础2费或升级后同时减费与保留。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为0警告、0错误，且明确关闭PCK导出与游戏目录复制。静态核对确认升级只执行 `EnergyCost.UpgradeBy(-1)`，卡农形态已从保留关键词置顶分支中移除，登记摘要同步为3（2）费。仍需实机核对基础/升级预览和实际费用；本条尚未发布，不添加勾选。

## 20260821-01 ✅

日期：20260821。

类型：卡牌数值平衡与文档纠正。

修改内容：撤销花葬、塞壬之歌、卡农形态和轮回在本轮开发中的临时平衡实验，四张牌恢复修改前的最终设计；「 鳥 」的基础伤害降低2点。

实现说明：花葬保持6（8）点伤害，塞壬之歌保持6（9）点格挡，卡农形态保持3（2）费且不具有保留。轮回恢复为1费、基础版具有虚无且升级移除虚无。「 鳥 」由24（30）点伤害调整为22（28），其按最终伤害一半生成基础音符、动态预览和视觉演出均不改变。内容登记与十语言工坊简介只记录最终净变化，不再把已撤销的中间实验列作玩家更新。

验证结果：内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为0警告、0错误，且明确关闭PCK导出与游戏目录复制。十语言工坊简介在2026-08-21平衡条目中合并点名「 鳥 」、「罪与罚」、「随之任之」、「点亮舞台」和「霓虹霓虹」，已撤销的四张牌未残留；所有简介低于8000个UTF-8字节且BBCode配对正确。仍需实机核对「 鳥 」伤害与轮回基础/升级预览；本条尚未发布，不添加勾选。

## 20260821-02 ✅

日期：20260821。

类型：本地化机制一致性修复与文档纠正。

修改内容：审计近期发生平衡调整的十张卡牌——注视、花葬、塞壬之歌、卡农形态、轮回、「 鳥 」、星辰导引、点亮舞台、霓虹霓虹与夜行少女——在十种游戏内语言中的描述是否仍与当前最终源码一致。

修复与改善：西班牙西语“点亮舞台”原描述把只在本场战斗内增加的攻击次数误写为永久增加，现改为明确的“durante este combate”。其余九张卡牌的动态数值、升级条件、费用变化与原生关键词显示均与源码一致；十语言均正确保留星辰导引的升级卡生成分支、注视的按减益抽牌变量，以及各卡当前伤害、格挡、演奏和音符机制。内容登记中注视仍写旧版伤害与格挡、霓虹霓虹仍写3点伤害、点亮舞台遗漏升级伤害的问题也一并纠正。

验证结果：十份 `cards.json` 均可解析，十张目标卡的格式器与变量集合跨语言一致，定向机制断言通过；内容登记以 warnings-as-errors 校验通过；固定 v0.107.1 与 v0.111.0 原版引用下的 Release 编译均为0警告、0错误，且明确关闭PCK导出与游戏目录复制。仍需实机核对西班牙西语“点亮舞台”的基础/升级预览和战斗内动态次数，以及星辰导引升级预览、轮回原生虚无关键词显示；本条尚未发布，不添加勾选。

## 20260821-03 ✅

日期：20260821。

类型：双版本构建与Steam工坊发布。

修改内容：以当前最终源码重新生成正式版0.107.1与测试版0.111.0双版本内容包，并更新Steam工坊项目 `3784531815`；十种语言标题与简介在内容上传后再次成对提交，防止元数据丢失。

构建与发布：内容登记warnings-as-errors校验通过；共享PCK、稳定启动器、两份原生payload和manifest均完成Release构建，工坊暂存目录只包含六个预期内容文件。PCK为146,761,112字节；两份payload的SHA-256与manifest一致。内容专用上传成功后，线上文件大小更新为148,363,951字节，内容句柄为 `6031962467734051375`，北京时间更新时间为2026-08-21 00:33:24。

验证结果：Steam公共接口返回结果码1且线上文件大小、内容句柄和更新时间均已变化；后续metadata-only提交由Steamworks逐一确认英语、简中、繁中、日语、韩语、法语、德语、西班牙西语、巴葡和俄语的标题与简介均与本地完全一致。没有部署到本地游戏目录，也没有推送GitHub。

## 20260821-04 ✅

日期：20260821。

类型：BUG修复。

问题表现：升级后的“立方棱镜”在0点能量时可以打出，但没有按描述对所有敌人造成1点伤害1次。

原因定位：`CubicPrism.OnPlay()` 在计算升级提供的 `X+1` 之前先用原始X值判断是否退出；当X为0时，流程直接返回，升级补偿没有机会进入伤害与次数计算。

修复与改善：先计算最终伤害与攻击次数 `X + 升级补偿`，再根据最终值决定是否结算。升级版X=0现在会播放既有棱镜折射与横向光束，并对所有可命中敌人造成1点伤害1次；普通版X=0仍不攻击。X大于0时的数值、演奏X、额外演奏、目标和费用支付保持不变。

验证结果：静态情景核对确认普通版X=0最终为0并退出，升级版X=0最终为1并进入全体多段伤害命令；内容登记以warnings-as-errors校验通过；固定v0.107.1与v0.111.0原版引用下的Release编译均为0警告、0错误。本改动已随2026-08-22最新内容包发布；仍需实机覆盖0能量普通/升级版、X=1、演奏重放与多人房间。

## 20260821-06 ✅

日期：20260821。

类型：界面信息改善。

修改内容：为MGR初始遗物“我的朋友”补充“基础音符”以及攻击、技能、能力、状态、诅咒五种基本音符的关键词悬浮提示。

实现说明：通过RitsuLib遗物模板的 `RegisteredKeywordIds` 依次登记六个MGR现有关键词，直接复用已经注册的图标和十语言标题、说明。遗物正文没有重复加入长文本，开战时实际生成攻击、技能和能力音符的效果也没有改变；状态音符和诅咒音符仅作为完整的基本音符规则参考显示。

验证结果：静态核对确认六个关键词ID及顺序正确；内容登记以warnings-as-errors校验通过；固定v0.107.1与v0.111.0原版引用下的Release编译均为0警告、0错误。本改动已随2026-08-22最新内容包发布；仍需实机检查角色选择、遗物栏和遗物查看界面的六项提示、顺序与排版。

## 20260822-01 ✅

日期：20260822。

类型：卡牌数值与机制平衡。

修改内容：“注视”恢复为根据目标不同负面状态获得格挡，数值调整为6（7）伤害、每种负面状态3（4）格挡；“乙女解剖”升级不再提高每张牌提供的格挡；“金枪鱼冲锋”移除抽牌并调整为8（11）点群体伤害；“电子天使”生成1费牌时采用白1、蓝0.8、金0.6的单卡相对权重。

实现说明：注视恢复旧版自定义动态格挡变量，对目标的非临时负面能力按ID去重，逐种给予格挡，并让卡面按当前目标实时显示经过格挡修正后的总值；升级同时使伤害6→7、单种格挡3→4。乙女解剖只移除升级的格挡+1，升级保留、基础每张4格挡、消耗与手牌转音符均不变。金枪鱼冲锋从初次攻击及每张被结束演奏牌的重复中统一删除抽牌，终结演奏和动态总攻击次数保留。电子天使在共享加权选择器中使用白5、蓝4、金3的整数权重，比例精确等同于1、0.8、0.6；候选仍只包含可在战斗中生成的1费MGR卡。

本地化：注视的简中、繁中、英语、德语、法语、日语、韩语、巴葡、俄语与西班牙西语恢复此前已发布的格挡译文；金枪鱼冲锋十语言均移除抽牌动作，其余两张牌的现有描述仍能准确表达修改后效果，无需增加概率或升级说明。本地化总指导与九份语言指导的源快照同步至 `2026-08-22-r17`。

验证结果：十份 `cards.json` 均可解析且key集合一致，注视与金枪鱼冲锋的目标格式器集合跨语言一致，旧抽牌变量均已清除；内容登记以warnings-as-errors校验通过；固定v0.107.1与v0.111.0原版引用下的Release编译均为0警告、0错误。本改动已随2026-08-22最新内容包发布；仍需实机覆盖注视动态格挡、乙女解剖升级、金枪鱼冲锋多次终结攻击及电子天使的长期权重分布。

## 20260822-02 ✅

日期：20260822。

类型：卡牌机制调整与本地化改善。

修改内容：“幽灵法则”从必须选择2（3）张弃牌改为选择至多2（3）张，允许玩家少选或直接跳过。

实现说明：弃牌堆选择器由固定数量改为最少0张、最多读取卡牌动态数值，并开启取消与手动确认；基础版上限为2张，升级版上限为3张。选择0张时仅跳过移牌步骤，随机诅咒仍会正常加入弃牌堆；费用、抽牌堆顶位置、基础消耗与升级移除消耗均不改变。十语言卡牌描述与选择提示同步改为“至多”，提示通过选择器原生 `MaxCount` 变量显示实际上限，内容登记中的旧版1（2）张摘要一并纠正。

验证结果：十份 `cards.json` 均可解析且key集合一致，目标描述和提示的动态变量跨语言一致；内容登记以warnings-as-errors校验通过，`git diff --check`无错误；固定v0.107.1与v0.111.0原版引用下的Release编译均为0警告、0错误。本改动已随2026-08-22最新内容包发布；仍需实机覆盖普通版/升级版选择0至上限张数、提前确认、跳过、移牌顺序与随机诅咒结算。

## 20260822-03 ✅

日期：20260822。

类型：卡牌数值与升级效果平衡。

修改内容：“点滴奇迹”的升级效果由“起音随机基础音符3→4、尾音剩余演奏次数+1→+2”改为“起音随机基础音符3→5”；升级前后尾音均只使剩余演奏次数+1。

实现说明：保留基础版0费、演奏3、起音3个随机基础音符及尾音+1；升级现在只把 `Cards` 动态变量提高2，不再升级 `TailPerformanceBonus`。十语言描述均复用 `Cards:diff()` 与 `TailPerformanceBonus:diff()`，会自动显示基础/升级的3（5）个音符和固定+1尾音，无需改写自然语言或新增条件分支。

验证结果：十语言 `cards.json` 均可解析，“点滴奇迹”描述均保留 `Cards:diff()`、`TailPerformanceBonus:diff()` 与 `Performance:diff()` 三个动态格式器；内容登记以warnings-as-errors校验通过，`git diff --check`无错误。固定v0.107.1与v0.111.0原版引用下的Release编译均为0警告、0错误。本改动已随2026-08-22最新内容包发布；仍需实机核对普通版/升级版起音数量、固定尾音增量、升级预览和演奏重放。

## 20260822-04 ✅

日期：20260822。

类型：卡牌机制调整。

修改内容：“银河电灯”改为基础版具有消耗、升级后移除消耗；升级不再获得保留。

实现说明：基础关键词集合加入原生 `Exhaust`，升级逻辑由添加 `Retain` 改为移除 `Exhaust`。十语言卡牌正文继续只描述移除音符并生成等量星空音符；消耗由原版关键词系统自动采用当前语言文本并显示在末行，升级后自动消失，因此无需在十份JSON中重复手写或维护十套关键词翻译。费用、稀有度、星空身份、音符转换数量与视觉演出均不改变。

验证结果：静态核对确认基础版只有消耗、升级版既无消耗也无保留；十份 `cards.json` 均可解析且目标正文没有重复关键词文本。内容登记以warnings-as-errors校验通过，`git diff --check`无错误；固定v0.107.1与v0.111.0原版引用下的Release编译均为0警告、0错误。本改动已随2026-08-22最新内容包发布；仍需实机确认十语言关键词显示及基础版进入消耗堆、升级版进入弃牌堆。

## 20260822-05 ✅

日期：20260822。

类型：卡牌数值与升级效果平衡。

修改内容：“森罗万象”的升级效果由演奏次数+1改为耗能降低1。基础版保持1费、演奏3；升级版改为0费、演奏仍为3。

实现说明：升级分支不再提高 `Performance` 动态变量，改为调用原版卡牌费用升级接口 `EnergyCost.UpgradeBy(-1)`。十语言正文继续通过 `{Performance:diff()}`显示固定的演奏3，费用变化由原版卡牌费用和升级预览直接呈现，无需在本地化正文中额外写一遍。内容登记同时纠正此前仍写作演奏2（3）的过期摘要。万象音符、先古稀有度、由古旧之牙转化获得的来源及演奏结算均不改变。

验证结果：十份 `cards.json` 均可解析且“森罗万象”描述各包含一个 `Performance:diff()`；静态断言确认基础演奏值为3、升级只执行 `EnergyCost.UpgradeBy(-1)`且不再修改演奏。内容登记以warnings-as-errors校验通过，`git diff --check`无错误；固定v0.107.1与v0.111.0原版引用下的Release编译均为0警告、0错误。本改动已随2026-08-22最新内容包发布；仍需实机核对基础版1费/演奏3、升级版0费/演奏3、升级预览以及古旧之牙转化后的升级状态。

## 20260822-06 ✅

日期：20260822。

类型：Steam工坊本地化与元数据发布。

修改内容：精简十种语言简介的日期式更新日志，并把2026-08-22的卡牌改动合并为一条平衡调整，完整点名注视、乙女解剖、金枪鱼冲锋、ʚ电子♡天使ɞ、幽灵法则、点滴奇迹、银河电灯和森罗万象；此前新增的一代MGRMod工坊链接一并发布。

实现说明：2026-08-18至2026-08-21的旧日志按同类修复、演出与平衡内容合并，保留日期和玩家向事项但删除过细展开。十种语言继续以简中为语义源并复用各自游戏内正式卡名。俄语简介由7963字节压缩到7266字节，其他语言也同步缩短。

验证与发布：十份简介的五个日期标题、2026-08-22单条结构、BBCode、链接唯一性、专名、永久排除项及8000字节限制全部通过；十份标题均为单行且低于128字节。随后以metadata-only模式将十种语言标题与简介成对提交到Steam工坊项目 `3784531815`，上传器逐语言验证线上标题和简介与本地一致。内容包、预览图、`workshop.json`与`SHA256SUMS.txt`的联合指纹在上传前后均为 `EF2DACB69D5070803AFA58C17AE8DE8AB247399F70661F9E8ED8F93F5B1BC94E`，未提交内容、标签、依赖、可见性或更新说明。

## 20260822-07 ✅

日期：20260822。

类型：双版本构建与Steam工坊发布。

修改内容：以当前最新源码与资源重新生成正式版v0.107.1和最新测试版v0.111.0双版本内容包，并通过content-only模式更新Steam工坊项目 `3784531815`。本次内容包包含2026-08-21上一版内容发布后完成的立方棱镜零能量修复、“我的朋友”关键词提示，以及2026-08-22的卡牌平衡与机制调整。

构建与发布：内容登记warnings-as-errors校验通过；共享PCK、稳定Loader、两份原生payload和manifest均完成Release构建，编译为0警告、0错误。工坊暂存目录严格包含六个预期文件，PCK为146,761,544字节；v0.107.1与v0.111.0 payload的SHA-256分别为 `B28DC82B6A1226D69D588E6EB3D767874FBE2AD718ECA3FD1C446B72520A6E81` 与 `64B5A98CED519311D78B73AA9C73018C81468561DA7D6C821660E399B56C0755`，均与manifest一致。

验证结果：内容专用上传器成功更新工坊条目。Steam公共接口返回结果码1，线上文件由148,363,951字节更新为148,366,431字节，内容句柄由 `6031962467734051375` 更新为 `860849899554711050`，北京时间更新时间为2026-08-22 15:38:22。上传前后本地标题、十语言简介、预览图和 `workshop.json` 的SHA-256保持不变，且临时上传目录已清理；未提交标签、依赖、可见性或更新说明，也未部署本地游戏目录或推送GitHub。

## 20260822-08 ✅

日期：20260822。

类型：Steam工坊本地化与元数据修正。

修改内容：补齐十种语言工坊简介中缺失的2026-08-16和2026-08-17更新日志，并纠正原先把2026-08-17修复与动画改善归入2026-08-18的问题。

实现说明：2026-08-16以初版角色、94张卡牌、11件专属遗物及音符/演奏机制概括；2026-08-17恢复“时间线之东”读档后永久音符成长、“涅染夜曲”诅咒/万象音符治疗，以及“黎明前线”“卡农形态”“电子天使”动画改善。2026-08-18明确保留“时间线之东”被降级后的永久成长修复及当天其余兼容、修复和平衡事项，十种语言均复用各自既有正式术语和内容名称。

验证与发布：十份简介均按2026-08-22至2026-08-16倒序包含七个日期，BBCode与链接检查通过，简介均低于8000字节，俄语最长为7853字节；十份标题均低于128字节。随后以metadata-only模式把十语言标题与简介成对提交到Steam工坊项目 `3784531815`，上传器逐语言验证线上值与本地一致。游戏内容包、预览图、`workshop.json`与`SHA256SUMS.txt`的联合指纹在上传前后均为 `201FB198763402CFCA8A935C3766C54CE2F083DCA312173B4635B86A745E40C5`，未提交内容、标签、依赖、可见性或更新说明。
